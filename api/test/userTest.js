/*

const test = require('node:test');
const assert = require('assert');
const express = require('express');
const router = express.Router();
const models = require('../models');

test('this test is run', async (t) => {
  // Within this test, all subtests are run by default.
  await t.test('running subtest');
});

test('Test para crear un Usuario', async (t) => {
  const user = await models.user.create({ email: "Mara", password: "123", id_alumno: null });
  assert.strictEqual("Mara", user.email);
  assert.strictEqual("123", user.password);
});


*/
